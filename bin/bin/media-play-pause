#!/usr/bin/env bash
set -euo pipefail

omarchy_shell="${OMARCHY_SHELL:-omarchy-shell}"

# The david.media service remembers the last source that started playback,
# including podcasts through mpv-mpris. A loaded episode is not a fallback.
exec "$omarchy_shell" media playPause
