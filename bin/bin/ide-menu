#!/bin/bash

# IDE selection menu using Omarchy Quattro's native menu.

choice=$(omarchy menu select "IDE…" "VS Code" "Cursor" -- --width 200 --maxheight 100 2>/dev/null) || exit 0

case "$choice" in
  "VS Code") uwsm-app -- code ;;
  "Cursor") uwsm-app -- cursor ;;
esac
